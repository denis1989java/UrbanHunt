meta {
  name: Add Hint
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/challenges/{{challengeId}}/hints
  body: json
  auth: none
}

body:json {
  {
    "content": "4 подсказка - смотрите наверх!",
    "publishedAt": "2026-03-31T10:00:00"
  }
}

vars:pre-request {
  challengeId: m4KE9sXWr10Fa2RcMsFY
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
